[project]
name = "zoox-fleet-skill-promotion-gate"
version = "0.0.1"
description = "Scaffold: Fleet Skill Promotion Gate (independent Zoox lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
